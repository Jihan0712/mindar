// Ecommerce config
// Checkout posts orders to `${MINDAR_API_BASE}/orders`.
// Default points at the optional local demo server in `backend/`.
window.MINDAR_API_BASE = window.MINDAR_API_BASE || 'http://localhost:8080';